# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config.
/.bundle

# Ignore the default SQLite database.
/db/*.sqlite3
/db/*.sqlite3-journal

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Commands used in byebug
.byebug_history

# Mac OS
.DS_Store

# Don't ever commit environment secrets stored in dot env files
/.env.production
/.env.development
/.env.test

# Never commit database backups to the codebase.
/db/backups/*
!/db/backups/.keep

# Ignore all the Puma related files given they are ephemeral and production only.
/puma/*
!/puma/.keep

# Never commit the production secrets.
.rbenv-vars

# Ignore all the temporary CDN related files, except the README.
/cdn/*
!/cdn/README.md